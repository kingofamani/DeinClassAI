# .env.litellm - Environment variables for LiteLLM service ONLY
# 請從您的主 .env 檔案中複製對應的實際值

# 必要變數
OPENAI_API_KEY=
LITELLM_ADMIN_KEY=

# LiteLLM 主控金鑰 (通常與 LITELLM_ADMIN_KEY 相同)
LITELLM_MASTER_KEY=

# UI 登入憑證
UI_USERNAME=
UI_PASSWORD=

# 加密金鑰 (用於加密/解密資料庫中的憑證)
# 重要：一旦設定就不能更改！請生成一個隨機字串
LITELLM_SALT_KEY=

# PostgreSQL 資料庫連線 URL
# 格式：postgresql://用戶名:密碼@主機:端口/資料庫名
DATABASE_URL=postgresql://litellm_user:litellm_password@postgres:5432/litellm

# 可選變數
PORT=4000
STORE_MODEL_IN_DB=True